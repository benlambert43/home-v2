import styled from "@emotion/styled";
import { colorTheme } from "../color/colorTheme";


export const Title2Typography = styled("div")({
  color: colorTheme.blackText,
  fontSize: 'calc(18px + .15vw)',
});