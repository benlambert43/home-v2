import styled from "@emotion/styled";
import { colorTheme } from "../color/colorTheme";

export const SubtitleTypography = styled("div")({
  color: colorTheme.grayText,
  fontSize: "calc(10px + .15vw)",
  paddingLeft: 2
});
