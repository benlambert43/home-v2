import styled from "@emotion/styled";
import { colorTheme } from "../color/colorTheme";


export const ParagraphTypography = styled("div")({
  color: colorTheme.blackText,
  fontSize: 'calc(16px + .15vw)',
});